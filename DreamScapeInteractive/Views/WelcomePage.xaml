<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DreamScapeInteractive.Views.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScapeInteractive.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#0F172A">

        <!-- Center Content -->
        <StackPanel 
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="20">

            <!-- App Title -->
            <TextBlock 
                Text="Welcome to DreamScape"
                FontSize="36"
                FontWeight="Bold"
                Foreground="White"
                HorizontalAlignment="Center"/>

            <TextBlock
                Text="Experience your dreams in interactive form"
                FontSize="18"
                Foreground="#CBD5E1"
                HorizontalAlignment="Center"
                TextAlignment="Center"
                Margin="0,0,0,20"/>

            <!-- Buttons -->
            <StackPanel Spacing="15">

                <!-- Login Button -->
                <Button Content="Login"
                        Width="220"
                        Height="50"
                        Background="#2563EB"
                        Foreground="White"
                        FontSize="16"
                        CornerRadius="10"
                        Click="Login_Click"/>

                <!-- Register Button -->
                <Button Content="Register"
                        Width="220"
                        Height="50"
                        Background="#10B981"
                        Foreground="White"
                        FontSize="16"
                        CornerRadius="10"
                        Click="Register_Click"/>

            </StackPanel>

        </StackPanel>

    </Grid>
</Page>
